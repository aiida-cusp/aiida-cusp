.. _user-guide-commands:

********
Commands
********

The AiiDA package provides a useful command line interface accessible through the ``verdi`` command.
To simplify certain tasks the plugin introduces additional commands to extend the default ``verdi`` command line interface.
In the following sections an overview of the set of commands added by the plugin and their usage is given.

.. toctree::
   :maxdepth: 1
   :caption: Added Commands:

   commands/cli_data_potcar.rst
